{
  "buildCommand": "rm -rf src/app/api && npm run build",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://jungsi.sean8320.dedyn.io/api/:path*"
    }
  ]
}
